# Fluency Engine Admin 0.9.3.4 Stabilization

- Uses a single Worker-reported version in the Admin UI.
- Adds release checks for stale version strings.
- Improves Google secret diagnostics and temporarily recognizes the observed GOIGLE_API_KEY misspelling while reporting a warning to rename it.
- Preserves the working complete-course ZIP image importer and canonical D1 schema.
